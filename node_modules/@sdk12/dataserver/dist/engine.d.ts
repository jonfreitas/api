import type { Options as ProtoOptions } from '@grpc/proto-loader';
import { Server } from '@grpc/grpc-js';
import { ServiceClient } from '@grpc/grpc-js/build/src/make-client';
type EngineOptions = {
    protos: ProtoOptions;
};
export declare const defaultOptions: EngineOptions;
export declare class Engine {
    constructor({ protos }?: Partial<EngineOptions>);
    protoOptions: ProtoOptions;
    server: Server;
    start: (address: string) => void;
    setClient: (remoteServer: string, pathProto: string) => ServiceClient;
    setServices: (services: any[]) => void;
    private getServiceClientFromProto;
    private getPackageString;
}
export {};
