"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.uptimeFormatted = exports.format = void 0;
function pad(s) {
    return s.toString().padStart(2, '0');
}
/**
 * Formats the input with the 00:00:00 mask
 * @param miliseconds Number of miliseconds passed
 *
 * @returns The time formatted
 */
function format(miliseconds) {
    const hours = pad(Math.floor(miliseconds / (60 * 60)));
    const minutes = pad(Math.floor((miliseconds % (60 * 60)) / 60));
    const seconds = pad(Math.floor(miliseconds % 60));
    return `${hours}:${minutes}:${seconds}`;
}
exports.format = format;
/**
 * Formats the input with the 00:00:00 mask
 * @returns
 */
function uptimeFormatted() {
    return format(process.uptime());
}
exports.uptimeFormatted = uptimeFormatted;
//# sourceMappingURL=uptime.js.map