import { Express } from 'express';
import graphqlHTTP from 'express-graphql';
import { IConfig, IGraphqlRouter, IRestRouter } from './interfaces';
export declare class Engine {
    server: Express;
    static: Function;
    graphql: typeof graphqlHTTP;
    routes: any[];
    defaultRoutes(health: Function): void;
    start(config: IConfig): void;
    configGrpcClient: (remoteServer: string, protoPath: string) => import("@grpc/grpc-js/build/src/make-client").ServiceClient;
    setRoutes(config: IConfig): void;
    parseRest(router: IRestRouter): void;
    parseGraphQL(router: IGraphqlRouter): void;
}
