import { Request, Response, NextFunction } from 'express';
export type jsonError = (message: string, status: number, code?: number | string) => void;
export interface CustomResponse extends Response {
    jsonError: jsonError;
}
export declare const JsonError: (request: Request, response: CustomResponse, next: NextFunction) => void;
